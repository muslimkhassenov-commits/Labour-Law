# PRD: «Миссия трудового права» — интерактивный учебный тренажёр

**Курс:** Трудовое право Республики Казахстан
**Занятие:** №1. Миссия трудового права
**Автор курса:** Хасенов Муслим Ханатович, PhD, Associate Professor, Maqsut Narikbayev University (MNU)
**Документ подготовлен:** август 2026
**Статус:** Прототип реализован (v1)

---

## 1. Контекст и предпосылки

Курс «Трудовое право Республики Казахстан» обновляется в рамках трансформации образовательных программ MNU. Принципы трансформации, определяющие требования к продукту:

- фундаментализация содержания;
- переход от запоминания норм к пониманию структуры отрасли;
- развитие профессионального юридического мышления;
- работа с неопределённостью;
- интеграция ИИ как инструмента и объекта критического анализа;
- оценивание не только итогового ответа, но и процесса мышления студента.

## 2. Проблема

Студенты часто воспринимают трудовое право как набор технических норм о трудовом договоре, рабочем времени, оплате труда и увольнении, не понимая, что это самостоятельная отрасль права со своей миссией, логикой, предметом, методом, функциями и системой. Обычная лекция или презентация не позволяет:

- заставить студента занять позицию и обосновать её;
- показать, как меняется позиция при появлении новых фактов;
- потренировать критическую оценку ответа ИИ по правовому вопросу;
- зафиксировать процесс мышления, а не только финальный ответ.

## 3. Цель продукта

Дать студенту (индивидуально на ноутбуке/телефоне или в аудитории с проектором) интерактивный тренажёр юридического мышления для первого занятия курса, который:

- показывает простую учебную модель сложной правовой системы без потери смысла;
- обучает через действие (выбор, классификация, обоснование), а не через чтение;
- раскрывает главный фундаментальный вопрос занятия: *«Почему трудовое право существует как самостоятельная отрасль права, если работник и работодатель формально могут заключить обычный гражданско-правовой договор?»*

### 3.1 Целевая аудитория
Студенты юридического факультета MNU, изучающие курс «Трудовое право Республики Казахстан». Продукт также используется преподавателем в аудитории (режим методических комментариев).

### 3.2 Язык
Русский.

## 4. Педагогические принципы (нефункциональные требования к опыту)

Сайт не должен просто сообщать студенту правильные ответы. Он обязан:

1. заставлять студента выбирать позицию;
2. требовать обоснования выбора;
3. показывать альтернативные аргументы;
4. позволять менять позицию при появлении новых фактов (кейс-стади);
5. учить отличать форму договора от существа отношений;
6. формировать понимание баланса интересов (а не механического равенства сторон);
7. тренировать критическую проверку ответа ИИ;
8. переносить фундаментальные идеи трудового права на новые ситуации (платформенная занятость, цифровой контроль).

## 5. Объём (Scope) v1

### 5.1 Технические требования
- React-приложение, без сложного backend.
- Все учебные данные (вопросы, кейсы, тайминг, тексты) вынесены в отдельные массивы/объекты — преподаватель может быстро редактировать содержание без изменения логики.
- Адаптивная вёрстка: ноутбук, планшет, телефон.
- Кнопка «Начать занятие».
- Progress bar прохождения занятия.
- Кнопка «Сбросить занятие».
- Возможность скопировать или скачать ответы студента в конце занятия (без сервера — экспорт в текстовый файл через Blob/clipboard).
- Не используются реальные персональные данные студентов.
- Не используются внешние изображения с возможным нарушением авторских прав — только CSS-графика, иконки, карточки, простые визуализации.
- Доступны два формата поставки:
  - React-компонент (артефакт / `.jsx`) для среды разработки;
  - самостоятельный `.html`-файл (React + Babel + Tailwind через CDN) для хостинга без сборки (Netlify / Vercel / GitHub Pages).

### 5.2 Стиль
Современный, академичный, визуально чистый, не скучный. Ощущение университетской образовательной платформы. Цвета: белый фон, тёмно-синий/графитовый текст, акценты голубого (MNU blue) и бордового. Карточки, схемы, интерактивные блоки, вопросы для голосования, мини-кейсы.

## 6. Функциональные требования по экранам

Продукт реализован как последовательность из 17 экранов с общим прогресс-баром и навигацией «Назад / Далее».

| # | Экран | Тип взаимодействия | Ключевое содержание |
|---|-------|--------------------|--------------------|
| 1 | Главный экран | Кнопка «Начать занятие» | Заголовок, подзаголовок, вводный текст, фундаментальный вопрос |
| 2 | План занятия | Клик по карточке → раскрытие подсказки | 8 пунктов плана; для каждого: зачем нужен / фундаментальный вопрос / связь с миссией |
| 3 | Почему это важно | Статистика + свободный ответ | 9,8 млн рабочая сила / 7,3 млн наёмные работники (~75%); вопрос об общественном интересе |
| 4 | Что такое труд | Классификация примеров по 3 аспектам | Исторический / социальный / экономический аспекты труда; 5 примеров для распределения |
| 5 | Самостоятельный и наёмный труд | Классификация 5 сценариев | Определения; комментарий после каждого выбора |
| 6 | Предмет трудового права | Множественный выбор (1 из 4) | Тезис о процессе труда как объекте правоотношения |
| 7 | Труд не товар | 4 аргумента + выбор ответа | Разбор нетоварной природы труда |
| 8 | Власть работодателя | Классификация 8 форм контроля по 3 категориям | Власть в пространстве / во времени / в цифровой среде |
| 9 | Платформенный курьер (кейс) | Пошаговый кейс: позиция → 5 новых фактов → повторная позиция после каждого факта → итоговый анализ | Признаки «за» трудовые и «за» гражданско-правовые отношения; принцип приоритета существа над формой |
| 10 | Миссия трудового права | Весы (визуализация) + слайдер баланса 0–100 | Интересы работника / работодателя; последствия крайних позиций и баланса |
| 11 | Функции трудового права | Классификация 8 институтов по 2 категориям | Общие (регулятивная/охранительная) и специальные (социальная/экономическая) функции; метафора корабля |
| 12 | История трудового права | Таймлайн из 5 этапов | От locatio-conductio operarum до цифровизации труда; вопрос на каждом этапе |
| 13 | Работа с неопределённостью | Сопоставление 5 сценариев с 7 видами неопределённости | Нормативная, фактическая, интерпретационная, технологическая, социальная, ценностная, стратегическая |
| 14 | ИИ как объект критического анализа | Чекбоксы разбора слабого ответа ИИ | 7 типовых слабых мест в юридическом ответе ИИ |
| 15 | Профессия юриста | Выбор роли + свободный аргумент | 9 профессиональных ролей; задание сформулировать аргумент по кейсу курьера |
| 16 | Финальная рефлексия | 5 текстовых полей | Вопросы, закрепляющие ключевые тезисы занятия |
| 17 | Финальный вывод | Копировать / скачать ответы, «пройти заново» | Итоговый текст о миссии трудового права; экспорт всех ответов студента |

### 6.1 Режим преподавателя
Скрытый режим, открывается кнопкой «Методические комментарии» (доступна в шапке на десктопе и плавающей кнопкой на мобильном). Содержит:
- тайминг занятия на 60 минут (9 интервалов, от вводного вопроса до финальной рефлексии);
- рекомендации, где остановиться для дискуссии;
- типичные ошибки студентов.

## 7. Пользовательские сценарии

1. **Студент проходит занятие индивидуально.** Открывает ссылку → жмёт «Начать занятие» → последовательно проходит 17 экранов, отвечая на вопросы и выполняя классификации → в конце копирует/скачивает файл с ответами для сдачи преподавателю.
2. **Преподаватель ведёт занятие в аудитории с проектором.** Открывает сайт → в нужный момент открывает «Методические комментарии», чтобы свериться с таймингом → останавливается на кейсе курьера и слайдере баланса для дискуссии.
3. **Студент/преподаватель перезапускает занятие.** Кнопка «Сбросить» очищает весь прогресс и возвращает на главный экран.

## 8. Метрики успеха (для будущих итераций)

Поскольку v1 без backend не собирает данные централизованно, метрики зависят от способа внедрения:
- доля студентов, дошедших до экрана 17 (по опросу/наблюдению в аудитории);
- качество и развёрнутость письменных ответов в кейсе курьера и финальной рефлексии (оценивается преподавателем по скачанным файлам);
- субъективная обратная связь: помогает ли тренажёр понять миссию отрасли лучше, чем лекция.

## 9. Ограничения v1 / не входит в объём

- Нет серверного хранения ответов студентов — экспорт только локально (файл/буфер обмена).
- Нет аутентификации и учёта конкретных студентов (осознанное решение — не используются персональные данные).
- Нет автоматической проверки правильности текстовых (свободных) ответов — только классификационные вопросы имеют встроенную обратную связь «верно/неверно».
- Нет аналитики использования на стороне преподавателя (например, агрегированной статистики по группе).

## 10. Возможные направления развития (v2+)

- Backend для сбора и агрегации ответов группы (с согласия студентов, без персональных данных или с псевдонимизацией).
- Drag-and-drop вместо кнопок-меток в блоках классификации.
- Экспорт ответов сразу в PDF/Word.
- Аналитика для преподавателя: сколько студентов выбрали каждый вариант, типичные текстовые ответы.
- Локализация на казахский и английский языки.
- Библиотека занятий №2, №3 и т.д. на той же технической платформе, с переиспользуемыми компонентами (Card, ChoiceButton, TextField, NavButtons и т.д.).

## 11. Технический стек (реализовано)

- **Артефакт-версия:** React (hooks: `useState`), иконки `lucide-react`, стилизация — Tailwind (базовые утилитарные классы).
- **Standalone HTML-версия:** тот же код React, скомпилированный в браузере через Babel Standalone; иконки заменены на inline SVG-шимы; React, ReactDOM и Tailwind подключены через CDN (unpkg / cdn.tailwindcss.com).
- Хостинг: любой статический хостинг (Netlify Drop, Vercel, GitHub Pages).

## 12. Файлы поставки (v1)

- `trudovoe-pravo-zanyatie1.jsx` — React-компонент (артефакт).
- `trudovoe-pravo-zanyatie1.html` — самостоятельный HTML-файл для деплоя без сборки.
- `prd.md` — настоящий документ.

---

## 13. Обновление v2 (август 2026) — расширенный модуль

По запросу курса модуль расширен до полноформатного образовательного продукта:
презентация + лендинг + тренажёры, в фирменном стиле MNU.

### 13.1 Состав v2

| Раздел | Содержание |
|---|---|
| Обложка (`cover`) | Заголовок, подзаголовок, фундаментальный вопрос, фото кампуса как фон, кнопки «Начать занятие» / «Перейти к тренажёрам» / «Финальная рефлексия», имя автора |
| Логика занятия (`roadmap`) | Схема: рынок труда → труд как категория → труд и капитал → прибавочная стоимость → наёмный труд → миссия → история → платформенная занятость → рефлексия |
| Презентация (`presentation`) | 30 слайдов: мысленный эксперимент, статистика рынка труда, политэкономия труда, весы, цитата Н.А. Шайкенова, корабль (руль/паруса), таймлайн истории, кейс платформенного курьера, главная идея |
| Тренажёры (`trainers`) | 7 интерактивных тренажёров: диагностический квиз, квалификация отношений по карточкам, весы (сортировка гарантий), руль/паруса (сортировка функций), кейс курьера, критика ответа ИИ, финальная рефлексия с экспортом ответов |
| Карта понимания (`map`) | Итоговая визуальная схема логики занятия |
| После занятия (`after`) | Блоки «повторить / подумать / мини-задание / AI-задание» + QR-зона на Canvas LMS |

### 13.2 Технический стек v2

- Один файл `index.html` (= `zanyatie1-v2.html`), vanilla JS без сборки и без React — весь контент вынесен в данные (`SLIDES`, `TRAINERS`, `QUIZ1`, `QUAL_CARDS`, `GUARANTEES` и т.д.) в начале `<script>`, отдельно от логики рендера.
- Брендинг: логотип и фото кампуса подключаются из `assets/mnu-logo.png` и `assets/mnu-campus.jpg`; при отсутствии файлов автоматически показывается SVG/CSS-заглушка в фирменных цветах (см. `assets/README.md`).
- Цвета: тёмно-синий `--navy`, графитовый `--graphite`, бордовый `--burgundy`, красный акцент `--red`, светлый фон `--paper`.
- QR-зоны генерируют код на лету через `qrcodejs` (CDN) по ссылке, введённой преподавателем; без интернета/ссылки показывается плейсхолдер.
- Прогресс и ответы студента сохраняются в `localStorage`; финальная рефлексия экспортируется в `.txt` через Blob (без бэкенда, как и в v1).

### 13.3 Файлы поставки (v2)

- `zanyatie1-v2.html` — рабочий модуль v2 (сохранён как предыдущая версия, больше не является точкой входа).
- `assets/README.md` — инструкция по добавлению логотипа и фото кампуса.
- Файлы v1 (`trudovoe-pravo-zanyatie1.jsx/html`) сохранены как более ранняя версия.

---

## 14. Обновление v3 (август 2026) — 40 слайдов, 12 тренажёров, видео и клавиатурный клик

Курс потребовал существенно более глубокого фундаментального содержания
(классическая политэкономия, диалектика, четыре промышленные революции, три
революции происхождения трудового права, квадрат регулирования рынка труда,
смежные отрасли, профессия юриста) и поддержки живого проведения занятия с
кликером в руках у преподавателя.

### 14.1 Новое содержание v3

- **Презентация — 40 слайдов** (было 30): добавлены блоки про Смита/Рикардо/Маркса,
  трудовую теорию стоимости, три закона диалектики (единство и борьба
  противоположностей, переход количества в качество, отрицание отрицания),
  таймлайн четырёх промышленных революций, три революции происхождения
  трудового права, интерактивный квадрат регулирования рынка труда (3×3,
  клик по ячейке раскрывает статус), сеть смежных отраслей права,
  4 профессиональные траектории юриста и 6 ключевых компетенций.
- **Тренажёры — 12** (было 7): добавлены квиз по политэкономии, сортировка
  «общность/противоречие интересов» (диалектика), сопоставление четырёх
  промышленных революций, сортировка по трём революциям, интерактивная
  матрица регулирования, кейсы по смежным отраслям права. AI-критика
  расширена до 10 типовых ошибок, финальная рефлексия — до 9 вопросов.
- **Видео**: на слайд 24 («гарантии как гири») и слайд 26 («корабль: руль и
  паруса») встроены реальные видеофайлы преподавателя —
  `assets/video-scales.mp4` и `assets/video-ship.mp4`. На слайд 38
  («финальный мысленный эксперимент») встроен `assets/video-world.mp4`
  («Мир, в котором нет трудового права»); QR-зона на этом слайде
  предзаполнена прямой ссылкой на файл на GitHub Pages
  (`.../Labour-Law/assets/video-world.mp4`) — работает после включения Pages.
- **Mentimeter**: QR-зона на слайде 18 предзаполнена реальной ссылкой
  преподавателя (`menti.com/ald5885ifci4`) и генерирует QR-код автоматически
  при открытии слайда — не нужно вводить вручную. Механизм обобщён в
  `autoRenderQrZones()`: любая QR-зона, созданная с 4-м аргументом
  (`prefillUrl`), рендерит код сама.

### 14.2 Пошаговый показ (clicker-friendly)

Часть слайдов (2, 13, 24, 29, 30, 34) раскрывается по шагам: каждое нажатие
«Далее» либо физического кликера сначала показывает следующий пункт текущего
слайда и только после полного раскрытия переходит на следующий слайд.
Реализовано через `state.stage[slideIndex]` и функции `advanceSlide()` /
`backSlide()`. Слайды, помеченные `maxStep`, автоматически поддерживают эту
логику — новый контент добавляется в `SLIDES` без изменения навигационного
кода.

Добавлена клавиатурная навигация (стрелки, Page Up/Down, пробел) — работает
с любым физическим кликером для презентаций, поскольку кликеры эмулируют
нажатия клавиш. Видео на слайдах помечено классом `.auto-video` и получает
`play()` сразу после того как слайд/шаг с ним становится видимым — запуск
происходит в том же обработчике клика/нажатия клавиши, поэтому не блокируется
политикой автовоспроизведения браузера.

### 14.3 Файлы поставки (v3)

- `index.html` / `zanyatie1-v3.html` — текущий рабочий модуль (идентичны,
  `index.html` — точка входа для хостинга).
- `assets/video-scales.mp4`, `assets/video-ship.mp4` — видеоролики
  преподавателя (весы / корабль), встроены в слайды 24 и 26.
- `assets/mnu-logo.png`, `assets/mnu-campus.jpg` — по-прежнему нужно добавить
  вручную (см. `assets/README.md`); до тех пор используется фирменная
  SVG/CSS-заглушка.
- `zanyatie1-v2.html` и файлы v1 сохранены как предыдущие версии.

---

## 15. Занятие №2 (сентябрь 2026) — «Метод и принципы трудового права»

Второй по счёту модуль курса, продолжающий фундаментальную логику занятия №1:
если миссия отвечает на вопрос «зачем?», то метод и принципы — на вопрос
«как и ради чего регулируется труд?». Сделан отдельным файлом, не затрагивая
готовое занятие №1.

### 15.1 Состав

- **Презентация — 35 слайдов**: проблемный кейс об отказе от гарантий →
  понятие метода → предписание/запрет/дозволение (виджет-светофор) →
  императивность/диспозитивность → недостаточность гражданско-правового и
  административного методов → частно-публичная природа → централизованное и
  локальное регулирование → in favorem (виджет-лестница гарантий) → единство
  и дифференциация → переход к принципам (механизм + компас) → понятие и
  функции принципов → классификация принципов (виджет-пирамида, 4 уровня) →
  формальное/фактическое равенство → социальная справедливость → защита
  слабой стороны → добросовестность и злоупотребление правом → принципы МОТ →
  критический разбор ст. 4 ТК РК → финальная карта.
- **Тренажёры — 11**: сортировка предписание/запрет/дозволение, кейсы
  императивность/диспозитивность, сортировка централизованное/локальное,
  in favorem (5 ситуаций с выбором «действует/не действует»), единство или
  дифференциация, «принцип, право или метод» (критика ст. 4 ТК РК),
  сопоставление принципов МОТ, добросовестность (кейсы с раскрытием
  эталонного ответа), метод и цифровая занятость (открытые вопросы),
  AI-критика (10 ошибок), финальная рефлексия (9 вопросов + экспорт ответов).
- Разделы «Карта понимания» и «После занятия» — по той же структуре, что и
  занятие №1, с QR-зонами на Canvas LMS, Трудовой кодекс РК и самостоятельное
  задание.

### 15.2 Новые визуальные виджеты (переиспользуемы для будущих занятий)

- `compassSVG()` / `mechanismSVG()` — компас (принципы) и шестерёнки
  (механизм метода), используются на обложке и слайде-переходе.
- `trafficLightSVG()` — светофор для триады предписание/запрет/дозволение.
- `ladderWidget(rungs)` — лестница гарантий снизу вверх (закон → локальный
  акт) для принципа in favorem.
- `pyramidWidget(levels)` — сужающаяся кверху пирамида для классификации
  принципов (до 4 уровней, задаётся `maxStep` для пошагового раскрытия).

Остальная инфраструктура (цветовые токены, шрифты, `bucketSorter`,
`revealBox`, `choiceListFull`, `qrZone` + `autoRenderQrZones`, пошаговый показ
через `state.stage`/`maxStep`, клавиатурная навигация для кликера, экспорт
ответов через `downloads`-capability с запасным вариантом на Blob/буфер
обмена) — скопирована один в один из занятия №1 для единообразия и лёгкости
поддержки.

### 15.3 Файлы поставки

- `zanyatie2/index.html` — рабочий модуль занятия №2. Логотип и фото кампуса
  подключаются из общей папки `../assets/` (та же, что и у занятия №1) —
  достаточно один раз добавить `mnu-logo.png` и `mnu-campus.jpg`, и оба
  занятия подхватят их автоматически.
- На GitHub Pages будет доступен по адресу
  `https://muslimkhassenov-commits.github.io/Labour-Law/zanyatie2/`.

---

## 16. Занятие №3 (сентябрь 2026) — «Источники и формула трудового права»

Третий модуль курса. Если занятие №1 отвечает на вопрос «зачем?», а занятие
№2 — «как регулирует?», то занятие №3 отвечает на вопрос «где искать нормы и
как проверить законность конкретного условия?». Сделан отдельным файлом, не
затрагивая занятия №1 и №2.

### 16.1 Состав

- **Презентация — 40 слайдов**: проблемный кейс о штрафах/компенсации/
  возмещении обучения → понятие источника права и источника трудового
  права → правовые семьи → иерархия действующего права РК (5 уровней) →
  Конституция → международные договоры → НПА → Трудовой кодекс → ст. 10 ТК
  РК → карта регуляторов (вертикальная схема) → централизованное,
  коллективно-договорное, локальное регулирование → уровни социального
  партнёрства (лестница) → трудовой договор как регулятор → тенденции
  источников → судебная практика → правовой обычай → пирамида юридической
  силы (8 уровней, пошаговое раскрытие) → формулы частного и публичного
  права → дуализм трудового права → формула трудового права → in favorem
  (лестница) → ст. 10 ТК РК как красная линия → алгоритм проверки документа
  (7 шагов) → **три судебных кейса** (штрафы, компенсация за увольнение,
  договор обучения) → сравнение исходов → как читать судебное решение →
  система из 4 фильтров законности → финальная карта.
- **Судебные кейсы — отдельный раздел навигации**: кнопка «Судебные кейсы» в
  шапке и на обложке ведёт сразу на слайд 33 (`goToCases()`), где расположены
  все три кейса подряд (слайды 33–35).
- **Тренажёры — 12**: сортировка по 7 источникам, «регулятор или кадровая
  практика» (клик-выбор с проверкой), **иерархия источников как ranking-
  упражнение** (кликом строится порядок, затем сверяется с эталоном и каждая
  позиция подсвечивается ✓/✕), формулы права (сортировка), in favorem (5
  ситуаций), три развёрнутых кейса с открытыми вопросами (штрафы,
  компенсация, обучение), разбор судебного решения, проверка локального акта
  по алгоритму (4 пункта с раскрытием эталона), AI-критика (10 ошибок),
  финальная рефлексия (10 вопросов).

### 16.2 Новые виджеты (переиспользуемы для будущих занятий)

- `pyramidWidget(levels)` обобщён: ширина уровня теперь считается по формуле
  `100 − i·(60/(n−1))`, поэтому пирамида одинаково хорошо работает и для 4
  уровней (занятие №2), и для 5 или 8 уровней (занятие №3) без изменения
  функции.
- `verticalFlow(items, {zoneIdx, final})` — вертикальная схема регулирования
  (переиспользует стили `.map-node`/`.map-arrow` из итоговой карты), с
  опциональной подсветкой «водораздела» (`zoneIdx`) и завершающим блоком
  (`final`). Используется и на слайде 15 («карта регуляторов»), и на
  итоговой карте понимания.
- `filterSVG()` — воронка-фильтр законности (источник → сила → in favorem →
  вывод), пара к `compassSVG()`.
- `rankingExercise(key, items)` + `pickRank`/`unRank`/`checkRank` —
  упражнение на восстановление иерархии кликом (без хрупкого HTML5
  drag-and-drop): элементы добавляются по одному в столбец, после заполнения
  всех позиций появляется кнопка «Проверить порядок», и каждая позиция
  подсвечивается зелёным/красным по сравнению с эталонным порядком.

Остальная инфраструктура (цветовые токены, `bucketSorter`, `revealBox`,
`choiceListFull`, `qrZone`+`autoRenderQrZones`, `ladderWidget`,
`compassSVG`/`mechanismSVG`, пошаговый показ через `state.stage`/`maxStep`,
клавиатурная навigация для кликера, экспорт ответов через
`downloads`-capability) скопирована из занятий №1–2 для единообразия.

### 16.3 Файлы поставки

- `zanyatie3/index.html` — рабочий модуль занятия №3. Логотип и фото кампуса
  подключаются из общей папки `../assets/`, как и у занятий №1–2.
- На GitHub Pages будет доступен по адресу
  `https://muslimkhassenov-commits.github.io/Labour-Law/zanyatie3/`.

---

## 17. Занятие №4 (сентябрь 2026) — «Международное регулирование труда. Наука трудового права»

Четвёртый модуль курса. Если занятия №1–3 отвечали на вопросы «зачем?»,
«как регулирует?» и «где искать нормы?», то занятие №4 выходит за пределы
национального кодекса: «почему труд стал международным вопросом и зачем
юристу наука трудового права?». Сделан отдельным файлом
(`zanyatie4/index.html`), не затрагивая занятия №1–3.

### 17.1 Состав

- **Презентация — 45 слайдов**: связь с занятиями 1–3 → проблемный кейс
  «конкуренция за счёт дешёвого труда» → три причины интернационализации
  труда (гуманитарная/социальная/экономическая, пошаговое раскрытие) →
  промышленная революция → первые фабричные законы → Роберт Оуэн → Чарльз
  Фредерик Хиндли → три мотива + QR-зона Mentimeter → Первая мировая война →
  Версальский договор 1919 года → девять версальских принципов (пошаговое
  раскрытие) → «труд не является товаром» → трудовые права как права
  человека → место международного трудового права в системе права → МОТ как
  организация → трипартизм (треугольная SVG-схема) → структура МОТ
  (конференция / совет / бюро) → Филадельфийская декларация → Нобелевская
  премия мира → достойный труд → международные трудовые стандарты →
  конвенции и рекомендации (жёсткое/мягкое право) → пирамида 402
  инструментов МОТ → пять фундаментальных принципов (пошаговое раскрытие,
  включая принцип безопасности и гигиены труда 2022 года) → 10
  фундаментальных конвенций по группам (№87/98, №29/105, №138/182,
  №100/111, №155/187) → приоритетные и технические конвенции → NORMLEX +
  QR-зона с реальной ссылкой `https://normlex.ilo.org/dyn` → система
  контроля МОТ (регулярный надзор + специальные процедуры) → Комитет
  экспертов и Комитет Конференции → свобода объединения как особая
  процедура → Казахстан и МОТ → проблема имплементации → переход к науке
  трудового права → наука трудового права как отрасль знания → почему труд
  особый объект науки → истоки науки (Германия, США) → Лотмар и Зинцхаймер →
  главная идея занятия.
- **Международные стандарты и наука трудового права — два новых
  навигационных перехода**: кнопки «К международным стандартам» и «К науке
  трудового права» в шапке и на обложке ведут напрямую на слайд 17 (МОТ как
  организация, `goToStandards()`) и слайд 41 (наука трудового права,
  `goToScience()`) соответственно — по аналогии с `goToCases()` занятия №3.
- **Тренажёры — 15**: кейс с выбором и объяснением (внутреннее дело
  государства или международная проблема), сортировка по трём мотивам,
  **множественный выбор** верных версальских принципов (новый виджет
  `multiSelectExercise`), сопоставление органов МОТ и функций, классификация
  жёсткого/мягкого права, сопоставление 5 принципов и 10 конвенций,
  два развёрнутых кейса с открытыми вопросами (принудительный труд,
  дискриминация), матрица «достойный труд», кейс о ратификации конвенции,
  **ranking-упражнение** «цепочка контроля МОТ» (7 шагов), открытый ответ о
  доктрине, сопоставление 10 учёных трудового права и их вклада (сортировка
  с 10 корзинами по фамилии), AI-критика (10 ошибок), финальная рефлексия
  (12 вопросов) с экспортом ответов в файл.

### 17.2 Новые виджеты (переиспользуемы для будущих занятий)

- `globeSVG(size)` и `triangleSVG(labels, size)` — глобус и равносторонний
  треугольник трипартизма с подписанными вершинами (viewBox подобран так,
  чтобы длинные подписи вроде «Работодатели» не обрезались по краю).
- `multiSelectExercise(key, items)` + `toggleMultiSel`/`checkMultiSel` —
  упражнение «отметьте все верные карточки»: чекбоксы, кнопка «Проверить»,
  после проверки верные позиции подсвечиваются зелёным, а неверно отмеченные
  или пропущенные — красным.
- `bucketSorter` использован с сортировкой до 10 корзин одновременно
  (тренажёр 13, учёные трудового права) — для читаемости кнопок-переходов
  внутри карточек метки корзин заданы в формате «Фамилия (Имя)», а не
  «Имя Фамилия», поскольку кнопка использует первое слово метки.

Остальная инфраструктура (цветовые токены, `pyramidWidget`, `verticalFlow`,
`rankingExercise`, `revealBox`, `choiceListFull`, `qrZone`+
`autoRenderQrZones`, `ladderWidget`, `compassSVG`, пошаговый показ через
`state.stage`/`maxStep`, клавиатурная навигация для кликера, экспорт ответов
через `downloads`-capability) скопирована из занятий №1–3 для единообразия.

### 17.3 Файлы поставки

- `zanyatie4/index.html` — рабочий модуль занятия №4. Логотип и фото кампуса
  подключаются из общей папки `../assets/`, как и у занятий №1–3.
- На GitHub Pages будет доступен по адресу
  `https://muslimkhassenov-commits.github.io/Labour-Law/zanyatie4/`.

---

## 18. Занятие №5 (сентябрь 2026) — «Государственное регулирование в области трудовых отношений. Контроль за соблюдением трудового законодательства»

Пятый модуль курса. Если занятия №1–4 отвечали на вопросы «зачем?», «как
регулирует?», «где искать нормы?» и «какой минимум признаётся
международно?», то занятие №5 отвечает на вопрос «как государство
превращает трудовые права из текста закона в реально работающие гарантии?».
Сделан отдельным файлом (`zanyatie5/index.html`), не затрагивая занятия
№1–4.

### 18.1 Состав

- **Презентация — 45 слайдов**: связь с занятиями 1–4 → проблемный кейс
  «кодекс есть, права нарушены» → главный вопрос (треугольник
  государство/работник/работодатель) → почему тема важна + QR-зона
  Mentimeter → граница трудового и административного права →
  государственная политика в области труда (статические/динамические
  средства, стратегические документы, проблема отсутствия единого
  документа, Национальный доклад о труде) → государственное регулирование
  как инструмент политики → минимальные социальные стандарты → система
  государственных органов (Правительство → МТСЗН → территориальные органы
  → инспекторы) → Комитет государственной инспекции труда → инспекция
  труда → Конвенции МОТ №81 и №129 + две QR-зоны → три задачи инспекции по
  МОТ (пошаговое раскрытие) → стандарты работы инспекции → проблема
  предварительного уведомления → ослабление инспекции vs риск для
  работников + QR-зона мини-опроса → правовой статус, права (9 пунктов,
  пошаговое раскрытие) и обязанности инспектора → 5 актов инспектора →
  предписание → статистика контроля 2025 → задолженность по зарплате
  2024/2025 → формы контроля (проверка vs профилактический контроль) →
  основания внеплановой проверки (9 пунктов, пошаговое раскрытие) →
  пределы проверки → порядок проверки (9-шаговый таймлайн) → сроки
  проверки → недействительность и обжалование → риск-ориентированный
  подход (шкала-градиент 0–100) → объективные критерии риска (3 группы,
  пошаговое раскрытие, цветовые risk-карточки) → виды нарушений → кейс
  «юрист между работодателем и инспекцией» → главная идея занятия + QR
  exit-ticket.
- **Пять новых навигационных переходов**: «К гос. политике» (`goToPolicy()`
  → слайд 7), «К системе органов» (`goToOrgans()` → слайд 17), «К инспекции
  труда» (`goToInspection()` → слайд 21), «К формам контроля»
  (`goToControlForms()` → слайд 34), «К риск-подходу» (`goToRisk()` →
  слайд 41) — по аналогии с `goToCases()`/`goToStandards()` предыдущих
  занятий.
- **Тренажёры — 16**: кейс с выбором (нужен ли контроль), сортировка
  «политика/регулирование/контроль» (11 карточек, 3 корзины), сопоставление
  органа и функции (6×6), множественный выбор «что требует МОТ от
  инспекции» и «обязанности инспектора», сравнение проверки и
  профилактического контроля (4 ситуации через `revealBox`), сортировка
  «может/не может быть основанием проверки», открытые вопросы «пределы
  проверки», сопоставление «ситуация → полномочие инспектора» (6×6),
  сопоставление «ситуация → акт инспектора» (5×5), сортировка по
  риск-уровню (10 субъектов, 3 корзины), два развёрнутых кейса с открытыми
  вопросами (зарплатная задолженность, несчастный случай), задание на
  формулирование сбалансированной правовой позиции, AI-критика (10
  ошибок), финальная рефлексия (15 вопросов) с экспортом ответов в файл.

### 18.2 Новые виджеты (переиспользуемы для будущих занятий)

- `shieldSVG(size)` — щит с галочкой (метафора «инспекция труда как щит
  трудовых прав»), использован на титульном слайде и на слайде о значении
  инспекции труда.
- `riskCard(title, body, level)` — карточка с цветовым кодированием риска
  (`high`/`medium`/`low` → бордово-красная/тёмно-синяя/серая рамка и фон),
  использована для наглядного отображения объективных критериев риска.
- `.risk-scale` (инлайн-разметка, без отдельной JS-функции) — горизонтальная
  трёхсегментная градиентная шкала 0–100 (низкий/средний/высокий риск) с
  шириной сегментов, пропорциональной диапазону баллов.
- `timelineStep`/`.timeline` (существовали в CSS с занятия №3, впервые
  реально использованы) — «дорожная карта проверки» из 9 пронумерованных
  шагов на слайде о порядке проведения проверки.
- `triangleSVG` переиспользован с новыми подписями («Государство» /
  «Работник» / «Работодатель») — треугольник баланса вместо трипартизма
  МОТ.

### 18.3 Файлы поставки

- `zanyatie5/index.html` — рабочий модуль занятия №5. Логотип и фото кампуса
  подключаются из общей папки `../assets/`, как и у занятий №1–4.
- На GitHub Pages будет доступен по адресу
  `https://muslimkhassenov-commits.github.io/Labour-Law/zanyatie5/`.

### 18.4 Обновление: слайд «Виды нарушений по санкциям» (после слайда 43)

По просьбе автора после слайда 43 («Виды нарушений» — грубые/значительные/
незначительные) добавлен новый слайд «Виды нарушений трудового
законодательства по санкциям» — трёхколоночная схема, воспроизводящая
содержание приложенного слайда из старой презентации автора (легаси-файл
`.ppt`, извлечён вручную парсингом бинарных text-атомов OLE-потока
`PowerPoint Document`, так как LibreOffice headless не смог открыть файл):
1. нарушения без санкций по УК РК и КоАП → государственный контроль →
   предписание → в случае неисполнения — переход к административной
   ответственности;
2. нарушения с санкциями по КоАП РК → протокол об административном
   правонарушении → постановление по делу (пример: ст. 462 КоАП о
   воспрепятствовании инспектору труда);
3. нарушения с санкциями по УК РК → дела частного обвинения (суд), дела
   частно-публичного обвинения (ОВД + суд), дела публичного обвинения.

Слайд вставлен в массив `SLIDES` как новый элемент (используются только уже
существующие хелперы `verticalFlow`/`numList`/`card`, новых виджетов не
добавлено). Модуль теперь содержит 46 слайдов; все существовавшие
навигационные переходы (`goToPolicy`/`goToOrgans`/`goToInspection`/
`goToControlForms`/`goToRisk`) указывают на индексы до точки вставки и не
требовали изменений.

---

## 19. Тема 6 (сентябрь 2026) — «Трудовое правоотношение» (единый модуль на 3 занятия)

Шестая тема курса — первый модуль, построенный не как одно занятие, а как
**единый интерактивный модуль на три полноценных занятия** (6.1, 6.2, 6.3)
с общей рамкой, единой судебной практикой, единым P2P-тренажёром и единой
финальной картой. Сделан отдельным файлом (`zanyatie6/index.html`), не
затрагивая занятия №1–5.

### 19.1 Архитектурное отличие от предыдущих модулей

В отличие от занятий №1–5 (один `SLIDES`-массив = одно занятие), здесь один
`SLIDES`-массив на 102 слайда охватывает три занятия сразу:
- Блок 1 (общая рамка темы) — слайды 1–5 (индексы 0–4);
- Занятие 6.1 «Понятие, структура и субъекты трудового правоотношения» —
  слайды 6–38 (индексы 5–37);
- Занятие 6.2 «Содержание трудового правоотношения: права, обязанности,
  представительство и работодательская власть» — слайды 39–69 (индексы
  38–68);
- Занятие 6.3 «Фактические и скрытые трудовые отношения. ГПХ. Судебная
  практика и P2P» — слайды 70–102 (индексы 69–101).

Три новых перехода `goTo61()`/`goTo62()`/`goTo63()` прыгают на индексы
5/38/69 соответственно — по аналогии с `goToStandards()`/`goToPolicy()` из
предыдущих занятий, но теперь адресуют не тематический раздел внутри
одного занятия, а границу между тремя занятиями внутри одной темы.

Помимо стандартных экранов (`cover`/`roadmap`/`presentation`/`trainers`/
`map`/`after`), добавлены два новых полноценных экрана:
- **`cases`** — раздел «Судебная практика» с 7 карточками дел и единой
  структурой детального просмотра (факты → позиция работника → позиция
  работодателя → вопрос суда с `revealBox` → признаки → доказательства →
  решение → урок для юриста), реализованной через общий хелпер
  `caseDetail(id, opts)`. Дело №6 (медработники ВКО) — особый случай:
  вместо `caseDetail` использует прямой рендер с «судебной лестницей»
  (три `timelineStep`: первая инстанция → апелляция → Верховный Суд) и
  отдельным блоком методологического вывода.
- **`p2p`** — центральный финальный блок темы: полноценный экран с фабулой
  спора, 7 ролевыми карточками (`.role-card`: работник, работодатель,
  инспекция труда, суд первой инстанции, апелляция, Верховный Суд,
  наблюдатели-эксперты), критериями оценки и 9 открытыми вопросами
  постфактум-рефлексии.

### 19.2 Новые виджеты (переиспользуемы для будущих занятий)

- `comparisonTable(headers, rows)` — обычная HTML-таблица в горизонтально
  прокручиваемом контейнере (`.table-wrap`/`table.cmp`), с тёмно-синей
  шапкой и зеброй строк. Использована для сравнения трудового договора и
  ГПХ по 11 критериям (слайд 82) и для карты различий между тремя
  судебными инстанциями по делу медработников ВКО (слайд 98 и в разделе
  «Судебная практика»).
- `appSVG(size)` — упрощённая иконка мобильного приложения (для метафоры
  платформенной занятости, кейс Glovo).
- `.role-card` — карточка роли для P2P-тренажёра (переиспользует
  `.card`-подобный стиль, но с более компактным списком).
- `caseDetail(id, opts)` — единый шаблон судебного дела, принимающий
  объект `{tag, title, facts, employeePosition, employerPosition,
  question, signs, evidence, decision, lesson}` и рендерящий его в
  консистентной структуре с `revealBox`-вопросом «как бы вы решили это
  дело?» перед показом решения суда.

Остальная инфраструктура (цветовые токены, `bucketSorter`,
`multiSelectExercise`, `rankingExercise`, `revealBox`, `choiceListFull`,
`verticalFlow`, `triangleSVG`, `scaleSVG`, `timelineStep`/`.timeline`,
`qrZone`+`autoRenderQrZones`, пошаговый показ через `state.stage`/
`maxStep`, клавиатурная навигация для кликера, экспорт ответов через
`downloads`-capability) скопирована из занятий №1–5 для единообразия.

### 19.3 Состав

- **102 слайда презентации**, включая: проблемный кейс всей темы, три
  «мини-тренажёра» встроенных прямо в презентацию (слайды 32, 33, 64,
  через `bucketSorter`/`revealBox` с отдельными state-ключами `mini32`/
  `mini33`/`mini64`, чтобы не пересекаться с состоянием тренажёров из
  раздела 6), пять судебных дел, встроенных в саму презентацию как
  повествовательные слайды (газорезчик, охранник — заняtie 6.1;
  модератор, бухгалтер — занятие 6.2; Caspian/переводчик, медработники
  ВКО с полной судебной лестницей на 5 слайдах, Glovo — занятие 6.3) и
  сравнительную таблицу трудового договора и ГПХ.
- **24 тренажёра** — включая множественный выбор, ranking-упражнение
  (сборка состава правоотношения), 10+ вариаций `bucketSorter`,
  тренажёры-повторения по каждому судебному делу (16–21) с отдельными
  открытыми вопросами и `revealBox`-моделью ответа, тренажёр-ссылка на
  P2P (22), AI-критика (23, 10 ошибок) и финальная рефлексия (24, 15
  вопросов) с экспортом в файл.
- **Раздел «Судебная практика»** — 7 карточек дел с единой структурой
  через `caseDetail`, включая судебную лестницу для дела медработников
  ВКО.
- **Раздел P2P** — фабула, 7 ролей, 12 критериев оценки, 9 вопросов после
  игры.

### 19.4 Найденный и исправленный баг

При Playwright-тестировании обнаружена коллизия меток корзин в
`bucketSorter` — кнопки формируются из первого слова метки
(`b.label.split(' ')[0]`), и там, где две корзины начинались с одного и
того же слова, кнопки становились неразличимы:
- тренажёр 3 (юридический факт): «Акт работодателя» и «Акт работника» →
  оба показывали «→Акт» — переименованы в «Работодательский акт» и
  «Заявление работника»;
- тренажёр 4 (субъект правоотношения): «Представитель работников» и
  «Представитель работодателей» → оба показывали «→Представитель» —
  переименованы в «Профсоюз / представитель работников» и «Объединение
  работодателей».

Проверено программно (`re`-поиск по всем массивам `buckets` на совпадение
первых слов меток) — коллизий больше нет.

### 19.5 Файлы поставки

- `zanyatie6/index.html` — рабочий модуль темы 6 (три занятия в одном
  файле). Логотип и фото кампуса подключаются из общей папки
  `../assets/`, как и у занятий №1–5.
- На GitHub Pages будет доступен по адресу
  `https://muslimkhassenov-commits.github.io/Labour-Law/zanyatie6/`.

### 19.6 Мелкая правка P2P-тренажёра

Роль 6 в P2P-разделе переименована с «Верховный Суд / кассация» на
просто «Кассация» по просьбе автора (единственное вхождение в файле).

---

## 20. Занятие №5: добавлен слайд «Нестабильность Трудового кодекса РК»

По просьбе автора после слайда 11 («Проблема отсутствия единого
документа») и перед слайдом 12 («Национальный доклад о труде») вставлен
новый слайд со статистической таблицей поправок в ТК РК за 2016–2026 годы
(число законов и число поправок по годам, 61 закон / 418 поправок за 10,5
лет, +947% к объёму кодекса; 124 поправки 19 законами за 2023–2025; 87
поправок 12 законами в 2026 году) — данные воспроизведены из приложенного
автором слайда старой презентации. Слайд логически продолжает вывод
предыдущего слайда о риске «фрагментарных реформ и нестабильности
законодательства», давая ему количественное подтверждение.

Для отображения таблицы (13 столбцов: показатель + 11 лет + «Итого») в
занятие №5 перенесены `comparisonTable(headers, rows)` и его CSS
(`.table-wrap`/`table.cmp`) из занятия №6, где этот хелпер уже
использовался для сравнения трудового договора и ГПХ. Таблица шире
контейнера слайда, поэтому прокручивается по горизонтали внутри
`.table-wrap{overflow-x:auto}` — проверено программно (scrollWidth >
clientWidth) и вручную через скролл в Playwright: все 13 столбцов,
включая «Итого» (61/418), доступны и совпадают с исходными данными.

Модуль занятия №5 теперь содержит 47 слайдов. Слайды после точки вставки
сдвинулись на один индекс, поэтому индексы у `goToOrgans`/
`goToInspection`/`goToControlForms`/`goToRisk` (все они указывали на
слайды после точки вставки) были пересчитаны на +1 (16→17, 20→21,
33→34, 40→41); `goToPolicy` (индекс 6, до точки вставки) не изменился.
Ошибка была обнаружена и исправлена в этой же правке, проверено через
Playwright — каждая функция теперь ведёт на верный слайд.
